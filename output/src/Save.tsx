import React from 'react'


interface ISaveProps {
    saveState: string,
    onSave?: () => void,
}

let xxxx = ''
const Save: React.FC<ISaveProps> = (props) => {
    return (
        <div>
        </div>
    )
}

export default Save
